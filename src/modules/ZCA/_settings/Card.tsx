import React from 'react';
import ZCACalculator from 'ZCA/common/ZCACalculator';

export interface IZCAConfigCard {
    calculator: ZCACalculator;
}

/**
 * Карточка настройки жца 
 * Принимает экземпляр калькулятора
 * @returns 
 */
function Card(props: IZCAConfigCard): React.ReactElement {
    return (
        <>
        
        </>
    );
}

export default Card;
